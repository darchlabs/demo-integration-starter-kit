{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.17.1"
  },
  "scripts": {
    "compile": "hardhat --config ./hardhat.config.js compile --verbose",
    "deploy": "hardhat --config ./hardhat.config.js run scripts/deploy.js",
    "export-artifacts": "cp -r artifacts/contracts/Demo.sol ../api/Demo.sol && cp ./contract-address.json ../api/contract-address.json"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
